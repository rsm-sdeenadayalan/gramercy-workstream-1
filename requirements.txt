# Gramercy sub-index pipelines — pinned runtime dependencies.
# Install with: pip install -r requirements.txt   (or `uv pip install -r requirements.txt`)
# Python 3.11+ recommended (tested with 3.14).

# ── Core ──────────────────────────────────────────────────────────────────────
anthropic==0.96.0
psycopg2-binary==2.9.11
python-dotenv==1.2.2
requests==2.33.1

# ── Data + parsing ────────────────────────────────────────────────────────────
pandas==3.0.2
numpy==2.4.4
openpyxl==3.1.5
beautifulsoup4==4.14.3
lxml==6.0.4
trafilatura==2.0.0
pdfplumber==0.11.9

# ── SI3 critical-mineral data ─────────────────────────────────────────────────
comtradeapicall==1.3.1
certifi>=2024.2.2          # macOS Py 3.14 ships without a system CA bundle

# ── Search backends (used by research_agent.py) ───────────────────────────────
tavily-python==0.7.23

# ── Optional: Playwright (used by a few collectors that need a real browser) ─
# Install browser binaries afterwards:  python -m playwright install chromium
playwright==1.58.0
